<?xml version="1.0" encoding="utf-8"?>
<!--
  ADIÇÕES AO AndroidManifest.xml
  ================================
  Cole os blocos abaixo dentro do seu arquivo
  android/app/src/main/AndroidManifest.xml

  1. As permissões vão ANTES de <application>
  2. O receiver vai DENTRO de <application>, após o <activity> existente
-->

<!-- ── 1. PERMISSÕES (antes de <application>) ────────────────────────────── -->
<!--
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
-->

<!-- ── 2. WIDGET RECEIVER (dentro de <application>) ─────────────────────── -->
<!--
    <receiver
        android:name=".CalendarWidget"
        android:exported="true">
        <intent-filter>
            <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
        </intent-filter>
        <meta-data
            android:name="android.appwidget.provider"
            android:resource="@xml/widget_info"/>
    </receiver>
-->

<!-- ── 3. BOOT RECEIVER para recriar notificações após reinício ──────────── -->
<!--
    <receiver
        android:name=".BootReceiver"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.BOOT_COMPLETED"/>
        </intent-filter>
    </receiver>
-->
